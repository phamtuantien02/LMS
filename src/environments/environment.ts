import { DEFAULT_CONFIG } from '../config/default/default';

export const environment = {
  production: false,
  apiUrl: DEFAULT_CONFIG.apiUrl,
  JWTUrl: DEFAULT_CONFIG.JWTUrl,
  config: DEFAULT_CONFIG
};

